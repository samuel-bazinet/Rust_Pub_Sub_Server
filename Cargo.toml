[package]
name = "rpss"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.25"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
simple_logger = "5.0.0"
rpss_utils = {git = "https://github.com/samuel-bazinet/rpss_utils", branch = "main", features = ["server"] }
